<%= form_for(@questionnaire) do |f| %>
    <%= render 'layouts/check_error' %>

    <div class="field">
	<%= f.label :name, "Questionnaire name:" %><br>
	<%= f.text_field :name %>
    </div>

    <%= f.fields_for :questions do |builder| %>
	<%= render 'question_fields', f: builder %>
    <% end %>

    <%= link_to_add_fields "Add new question", f, :questions %>

    <br>
    <br>
    <div class="actions">
	<%= f.submit %>
    </div>

<% end %>
